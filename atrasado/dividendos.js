function divisao(fristNum, secondNum, razao){
    const total = fristNum/secondNum;
    const total2 = fristNum%secondNum;
    const result = `R: ${fristNum}/${secondNum}= ${total}`
    const razao = total2;
}